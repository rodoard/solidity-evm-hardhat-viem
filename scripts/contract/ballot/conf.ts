const PROPOSALS: string[] = ["Proposal 1", "Proposal 2", "Proposal 3"];
const BALLOT_JSON = "../../../artifacts/contracts/Ballot.sol/Ballot.json"

const proposals = () => PROPOSALS
const ballotJSON = () => BALLOT_JSON

export {
  proposals,
  ballotJSON
}